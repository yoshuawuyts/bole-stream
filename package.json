{
  "name": "bole-stream",
  "version": "2.0.1",
  "description": "Stream data into a bole logger",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "deps:pkg": "ncu",
    "deps:update": "ncu -a",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/bole-stream",
  "keywords": [
    "stream",
    "bole",
    "ndjson",
    "log"
  ],
  "license": "MIT",
  "dependencies": {
    "JSONStream": "^1.0.6",
    "bole": "^2.0.0",
    "pump": "^1.0.0",
    "through2": "^2.0.0"
  },
  "devDependencies": {
    "bl": "^1.0.0",
    "dependency-check": "^2.5.1",
    "istanbul": "^0.3.20",
    "ndjson": "^1.4.2",
    "npm-check-updates": "^2.2.3",
    "standard": "^5.3.1",
    "tape": "^4.2.0"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
